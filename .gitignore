# --- Secrets / env (never commit real values) ---
/.env
backend/.env
database/.env
frontend/.env.local
frontend/.env
frontend/.env.production

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/

# Local image-upload stub (development only — see backend/s3_images.py)
backend/uploads/

# --- Node / Next ---
node_modules/
frontend/.next/
frontend/out/
npm-debug.log*

# --- Misc ---
.DS_Store

# Build-handoff scratch file written by scripts/docker-build.sh
scripts/.last-build-tag
